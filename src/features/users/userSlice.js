/* eslint-disable no-empty-pattern */
import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    
}

const userSlicce = createSlice({
    name: 'user',
    initialState,
    reducers: {
        
    }
})

export const { } =userSlicce.actions;
export default userSlicce.reducer;