/* eslint-disable no-empty-pattern */
import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    
}

const conversationsSlicce = createSlice({
    name: 'conversations',
    initialState,
    reducers: {
        
    }
})

export const { } =conversationsSlicce.actions;
export default conversationsSlicce.reducer;